import * as actions from '../constants/actionTypes';
import objectAssign from 'object-assign';
import initialState from './initialState';

export default function logbooksReducer(state = initialState.logbooks, action) {
  switch (action.type) {

    default:
      return state;
  }
}
